sudo: false
language: node_js
node_js:
  - "0.10"
cache:
  directories:
  - node_modules
before_script:
  - "export DISPLAY=:99.0"
  - sh -e /etc/init.d/xvfb start - nohup bash -c
  - npm install -g grunt-cli
script:
  - "grunt"
